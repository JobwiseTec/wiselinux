
PackageKit                  # updates automáticos de RPM — conflita com o modelo bootc
fedora-flathub-remote       # remoto "Fedora Flatpaks" — deixamos só o Flathub no Discover
kcharselect                 # Seletor de caracteres (removido do @kde-desktop)
krfb                        # Compartilhamento de tela VNC do KDE (removido do @kde-desktop)
kwrite                      # Editor de texto simples do KDE (removido do @kde-desktop)
plasma-browser-integration  # integração com navegador — não utilizada
firewall-config             # GUI GTK de firewall — usamos plasma-firewall (KDE)
mariadb                     # Banco de dados MariaDB (servidor) — não utilizado
mariadb-server              # Servidor MariaDB (não utilizado)
mariadb-common              # Arquivos comuns do MariaDB (não utilizado)
mariadb-errmsg              # Mensagens de erro do MariaDB (não utilizado)
mariadb-connector-c         # Conector C do MariaDB (não utilizado)
mariadb-connector-c-config  # Config do conector C do MariaDB (não utilizado)
mariadb-backup              # Ferramenta de backup do MariaDB (não utilizado)
mariadb-cracklib-password-check  # Checagem de senha cracklib do MariaDB (não utilizado)
mariadb-gssapi-server       # Suporte GSSAPI do MariaDB (não utilizado) 